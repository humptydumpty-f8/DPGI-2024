<Window x:Class="_42_CheckBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_42_CheckBox"
        mc:Ignorable="d"
        Title="Lection 4-2" Height="450" Width="800">
    <Grid>
        <StackPanel x:Name="stackPanel" VerticalAlignment="Center" HorizontalAlignment="Center">
            <CheckBox x:Name="checkBox1" IsThreeState="True" IsChecked="False" Height="20" Content="_Не відмічено" 
                      Unchecked="checkBox1_Unchecked" Checked="checkBox1_Checked" Indeterminate="checkBox1_Indeterminate"/>
            <CheckBox x:Name="checkBox2" IsThreeState="True" IsChecked="True" Height="20" Content="_Відмічено" />
            <CheckBox x:Name="checkBox3" IsThreeState="True" IsChecked="{x:Null}" Height="20" Content="Не ви_значено"/>
        </StackPanel>
    </Grid>
</Window>
